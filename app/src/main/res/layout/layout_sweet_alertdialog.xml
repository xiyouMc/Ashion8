<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="290dp"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:background="@drawable/dialog_sweet_backgroud"
    android:padding="10dp">
    <ImageView
        android:id="@+id/custom_image"
        android:layout_width="53dp"
        android:layout_height="53dp"
        android:layout_marginTop="5dp"
        android:contentDescription="@string/app_name"
        android:scaleType="fitCenter"
        android:background="@drawable/ic_launcher"/>
    <TextView
        android:id="@+id/dialog_title_view"        android:text="@string/dialog_title"
        style="@style/dialog_title_textview" />

    <TextView
        android:id="@+id/dialog_message_view"
        style="@style/dialog_message_textview" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="center">

        <Button
            android:id="@+id/dialog_negative_btn"
            style="@style/dialog_blue_button"
            android:background="@drawable/gray_button_background" />

        <Button
            android:id="@+id/dialog_positive_btn"
            style="@style/dialog_blue_button" />
    </LinearLayout>
</LinearLayout>
